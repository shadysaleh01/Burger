<li>
   {{burger_name}}
   {{#if devoured}}
   <button class="change-devoured btn btn-outline-danger" data-id="{{id}}" data-devoured="{{devoured}}">

      Eat Me!
      {{/if}}
   </button>
   <button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>